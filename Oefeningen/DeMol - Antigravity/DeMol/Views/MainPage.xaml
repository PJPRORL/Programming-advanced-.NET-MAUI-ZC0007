<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DeMol.Views.MainPage"
             xmlns:vm="clr-namespace:DeMol.ViewModels"
             xmlns:model="clr-namespace:DeMol.Models"
             x:DataType="vm:MainPageViewModel"
             Title="{Binding Title}"
             BackgroundColor="#FDFDF0">
    
    <Grid RowDefinitions="Auto, Auto, *, Auto" Padding="20">
        
        <Label Text="De Mol" 
               FontSize="80"
               TextColor="Black"
               HorizontalOptions="Center" 
               Margin="0,20,0,20"/>

        <Label Grid.Row="1"
               Text="Selecteer je naam:"
               FontSize="20"
               TextColor="Black"
               HorizontalOptions="Center"
               Margin="0,0,0,10"/>

        <CollectionView Grid.Row="2"
                        ItemsSource="{Binding Deelnemers}"
                        SelectionMode="Single"
                        SelectedItem="{Binding SelectedDeelnemer}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="10">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup Name="CommonStates">
                                <VisualState Name="Normal" />
                                <VisualState Name="Selected">
                                    <VisualState.Setters>
                                        <Setter Property="BackgroundColor" Value="#E0E0E0" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <Label Text="{Binding SelectedDeelnemer.VolledigeNaam}"
                               FontSize="24"
                               TextColor="Red"
                               HorizontalOptions="Center"/>
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <Button Grid.Row="3"
                Text="Vragen beantwoorden" 
                Command="{Binding StartCommand}"
                BackgroundColor="{StaticResource Primary}"
                TextColor="White"
                CornerRadius="10"
                HeightRequest="50"
                FontSize="16"
                Margin="0,20,0,20"
                HorizontalOptions="Center"
                WidthRequest="250"/>
    </Grid>
</ContentPage>